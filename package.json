{
  "name": "redis-promise-memoizer",
  "version": "0.0.1",
  "description": "A promise-based memoizer using redis as a shared cache, with TTL.",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/mocha/bin/mocha --reporter spec --timeout 5000"
  },
  "repository": "git@github.com:dclucas/redis-memoizer.git",
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.1.1",
    "ioredis": "^1.14.0"
  },
  "devDependencies": {
    "mocha": "^2.3.4",
    "should": "^8.0.2"
  }
}
